import {StyleSheet} from "react-native"

const SolutionStyles = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center',
        marginTop: Device.height / 54
    },
    content: {
        fontFamily: 'Bangla Sangam MN',
        fontSize: 18,
        textAlign: 'center'
    },
})

export {SolutionStyles}